# F1 Scenario 2: Coverage Drop Detection (Should Fail)
# Date: 2026-03-01

## Status: VERIFIED VIA UNIT TESTS (not CI)

## Why CI-level testing is not possible yet
- `unified-coverage.json` does NOT exist on `main` branch
- Baseline comparison is skipped when baseline file is missing
- This is by design: first merge creates baseline, subsequent PRs compare against it
- The baseline update step runs ONLY on push to main (not PRs):
  `if: github.ref == 'refs/heads/main' && github.event_name == 'push'`

## Unit Test Evidence (50/50 passed)
Baseline comparison tests (8 specific tests):

1. test_passes_when_coverage_equals_baseline: PASSED
   - Coverage 85.00% == baseline 85.00% -> exit 0

2. test_fails_when_unified_drops_below_baseline: PASSED
   - Coverage 84.99% < baseline 85.00% -> exit 1
   - Output: '❌ Unified coverage 84.99% is below baseline 85.00%'

3. test_fails_when_backend_drops_despite_unified_ok: PASSED
   - Unified ok but backend component drops -> exit 1
   - Output: '❌ backend coverage ... is below baseline ...'

4. test_fails_when_frontend_drops: PASSED
   - Frontend component drops -> exit 1

5. test_fails_when_scripts_drops: PASSED
   - Scripts component drops -> exit 1

6. test_passes_when_all_components_improve: PASSED
   - All components improve -> exit 0

7. test_skips_baseline_check_when_file_missing: PASSED
   - No baseline file -> comparison skipped, exit 0

8. test_baseline_file_path_configurable: PASSED
   - BASELINE_FILE env var controls path

## Expected CI Behavior After Merge
1. First push to main: baseline created, committed with [skip ci]
2. Subsequent PRs: coverage compared against baseline
3. If any component drops: CI fails with '❌ ... is below baseline ...'
4. If all maintained/improved: CI passes

## Baseline Update Logic (Verified in CI yml)
- Condition: `if: github.ref == 'refs/heads/main' && github.event_name == 'push'`
- Uses `! git diff --staged --quiet` (correctly checks for CHANGES)
- Commits with [skip ci] to prevent infinite loop
- Uses BASELINE_UPDATE_PAT secret for push

## Verdict: ✅ PASS - Drop detection verified via unit tests;
## CI integration deferred until baseline exists on main