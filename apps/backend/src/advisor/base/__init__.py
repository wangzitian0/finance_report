"""``advisor.base`` — the pure core: prompt language, constants, guardrails.

Downward-only (mechanism A): nothing here imports the package's own
``extension/`` or ``data/`` layers, other domains, or I/O infrastructure.
"""
