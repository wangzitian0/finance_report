# Shared Shell Helpers

This directory contains shell implementations and shared shell functions for
command entry points in `tools/`.

Run shell commands through `tools/*.sh`; keep reusable shell logic here.
