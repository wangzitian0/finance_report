# DO NOT edit this file manually - run tools/generate_ac_registry.py
# Explicit registry entries not derived from current EPIC text.
#
# Empty since the migration closeout (#1719). Formerly 157 deprecated
# placeholders, then a single e2e-only exception (AC8.2.6, password reset);
# that last entry was cleared because its only claimed proof
# (`test_password_reset_flow`) no longer exists in the repo — a stale
# override, not a live exception. Package contract roadmaps are the
# authoritative AC source; EPIC tables feed only explicitly marked residue
# rows (see tests/tooling/test_epic_residue_ratchet.py).
version: '1.0'
groups: {}
