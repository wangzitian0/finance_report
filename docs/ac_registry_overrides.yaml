# DO NOT edit this file manually - run tools/generate_ac_registry.py
# Explicit registry entries not derived from current EPIC text.
#
# Formerly 157 deprecated placeholders; 155 live ACs were promoted into their
# EPIC docs and 2 dead IDs dropped (AC8.13.100 superseded by AC8.13.98,
# AC9.3.7 removed). The single remaining entry is a genuine exception: it is
# covered only by an end-to-end test (no PR-CI unit/integration coverage), so it
# stays out of the mandatory traceability gate until CI coverage exists.
version: '1.0'
groups:
  AC8:
    AC8.2:
      - id: AC8.2.6
        epic: 8
        epic_name: testing-strategy
        description: Password reset flow (covered by e2e test_password_reset_flow only; no PR-CI coverage yet)
        status: deprecated
        mandatory: false
