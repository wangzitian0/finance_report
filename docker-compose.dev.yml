services:
  postgres:
    ports:
      - "5432:5432"

  redis:
    ports:
      - "6379:6379"

  backend:
    # Backend port usually not needed on host unless debugging directly
    ports:
      - "8000:8000"

  frontend:
    ports:
      - "3000:3000"
